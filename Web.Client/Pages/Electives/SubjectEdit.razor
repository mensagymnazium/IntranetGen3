<HxOffcanvas @ref="offcanvasComponent" Title="@title" Size="OffcanvasSize.Large">
    <BodyTemplate>
        <EditForm EditContext="editContext">
            <FluentValidationValidator />
            <HxInputText @bind-Value="model.Name" Label="Název" />
            <HxInputTextArea @bind-Value="model.Description" Label="Popis" rows="10" style="height:100%;" />
            <HxInputNumber @bind-Value="model.Capacity" Label="Kapacita" Hint="Ponechte prázné pro neomezenou kapacitu" Placeholder="Neomezená" />
            <TeacherMultiPicker @bind-Value="model.TeacherIds" Label="Vyučující" />
            <ScheduleTimeSlotPicker @bind-Day="model.ScheduleDayOfWeek" @bind-Slot="model.ScheduleSlotInDay" />
            <GradeMultiPicker @bind-Value="model.GradeIds" Label="Ročník(y)" />
            <SubjectCategoryPicker @bind-Value="model.CategoryId" Label="Skupina" />
            <EducationalAreaMultiPicker @bind-Value="model.EducationalAreaIds" Label="Vzdělávací oblast(i)" />
            <HxSwitch @bind-Value="model.CanRegisterRepeatedly" Label="Lze zapsat opakovaně" />
            <HxInputNumber @bind-Value="model.HoursPerWeek" Label="Dotované hodiny týdně" />
        </EditForm>
    </BodyTemplate>
    <FooterTemplate>
		<SaveButton EditContext="editContext" IsNew="(model.Id == default)" OnValidClick="HandleValidSubmit" />
    </FooterTemplate>
</HxOffcanvas>